id: store
name: Store
description: Browse and install plugins, apps, themes, and components from the community
icon: 🏪
version: 1.0.0

# Store app doesn't render entities - it's a standalone UI
views: []

# Store is accessed directly, not through entity routing
standalone: true
